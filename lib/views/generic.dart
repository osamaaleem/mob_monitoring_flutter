import 'package:flutter/material.dart';

class Generic extends StatelessWidget {
  const Generic({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Fuck You!"),
      ),
    );
  }
}
